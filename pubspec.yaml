name: pisatel_test_project
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # ui
  # cupertino_icons: ^0.1.3
  shimmer: ^1.1.1
  # persistency
  hive_flutter: ^0.3.1
  hive: ^1.4.4
  # state management
  bloc: ^6.0.3
  flutter_bloc: ^6.0.5
  # network
  dio: ^3.0.10
  cached_network_image: ^2.3.2+1
  # rxdart: ^0.24.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^0.7.1
  build_runner: any

flutter:
  uses-material-design: true
# assets:
#   - assets/

